# floor division (버림나눗셈)
# 소수점을 버릴 때 // 를 사용한다.
print (7 // 2)

# 버림 나눗셈을 할 때 둘중 하나가 소수점이면 결과값도 소수점이 나온다.
# 둘 다 소수점일 경우에도 결과값은 소수점이 나온다.
print(8.0 // 3)

# round 소수점 없애는 함수이다.
print(round(3.141561345132,4))